// Plain-JS anchor — proves the TS kinds read .js too (PRD §5.1: ts-const-array
// "works for JS too"; kinds describe declaration shape, not file type).
// Concept K-114. Expected clean extraction.
export const LOYALTY_TIERS = ['bronze', 'silver', 'gold', 'platinum'];
