# Plant store (UCS-1159) — duplicate accession ID.
# Both leaves below are WELL-FORMED and both mint K-000001. The defect is in
# the VALUE of the identity, not the shape of any record: nothing here is
# malformed, so nothing hides behind a schema error. The loader refuses the
# second mint (`duplicate-id`, exit 2), which is exactly the tabulated
# expected finding — see fixtures/ts-app/FIXTURE.md.
schema-version: 2
store: knowledge
entries:
  - id: K-000001
    title: Adding a new export format
    file: product/100.1-adding-a-new-export-format.md
  - id: K-000001
    title: Registering a new export format
    file: product/100.2-registering-a-new-export-format.md
