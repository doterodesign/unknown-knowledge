{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:unknown-knowledge:schema:catalog",
  "title": "Store catalog (_catalog.yaml)",
  "description": "Navigational pointers into the three content stores. Canonical IDs and same-kind qualified proposal keys use the active installation namespace; proposal rows remain authoring references and allocate no canonical identity. Shape, actual target agreement and ledger corroboration are reader and structural-validator checks.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema-version", "store", "entries"],
  "properties": {
    "schema-version": { "type": "integer", "enum": [2] },
    "store": { "enum": ["ontology", "knowledge", "decisions"] },
    "entries": {
      "type": "array",
      "items": { "$ref": "#/$defs/catalogEntry" }
    }
  },
  "$defs": {
    "catalogEntry": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "title", "file"],
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "file": {
          "type": "string",
          "description": "Path relative to the store root, or a documented pending marker while the record is being imported."
        }
      }
    }
  }
}
