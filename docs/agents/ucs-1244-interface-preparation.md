# UCS-1244 preparation — decision and documentation audit

## Combined interface checkpoint — 2026-09-21

The frozen combined source at `local-history:uk-query-tree-interface-sjwyg17l`
included typed material promotion, structural tree preview, query output v2 and
the two strict retirement/split refusal-consumer corrections. The independent
checkpoint ran the four interface suites, specialized Subject/view/intent CLIs,
copied installation, module loading and exit-code contract tests. It finished
**109/112 passed**, exit 1, in **11750.629208 ms**. All interface, official-SDK,
specialized CLI, copied-installation and module-load cases passed. The aggregate
is preserved as failed; it is not reported as 112/112.

Two exit-code failures exposed the generic crash test's missing MCP arguments
and its required shared-parser import convention. Source review also found the
same test assumptions for the explicit request-file CLI, hidden behind the first
failure. Both now use `parseArgs as parseFlags`; crash controls supply valid
arguments and inject only the actual main-time package/request access. The
internal-failure and exact injected-marker assertions remain required. The third
failure was the exported copy's missing Git metadata: the tracked-fixture audit
could not run. Its findings expectations remain unchanged.

Main reproduced the two interface test failures in the Git checkout, where the
audit control passed, then ran only the corrected exit-code suite and directly
affected core interface suite: **14/14 passed**, **1641.377166 ms**, Node 24.19.0.
Receipts: `local-history:unknown-knowledge-interface-exit-contract-red.log` and
`local-history:unknown-knowledge-interface-exit-contract-green.log`. The independent
checkpoint's `combined-interface-parity.log` and pre/post manifests remain in the
P11 artifact directory below. All 1815 source files, 1069 dependency files and
both executables matched its freeze after execution, with no extra files. The
correction is a separate receipt, not a rewritten frozen run. This closes the
bounded eleven-operation adapter verification; it does not complete domain
lifecycle implementation or the spec's full acceptance matrix.

## Structural preview increment — 2026-09-21

Integration approved the finite adapter frontier of the ten existing shared
read-only operations plus `subject.tree`, P4's per-operation output versions and
actual integrated parity/documentation closure. Historical scope-hold statements
below remain records of earlier preparation, not current restrictions. Domain
mutation features stay required under their owners; exposing mutation tools or
completing the full adjacent project is not a P11 prerequisite.

`subject.tree` now binds the actual P6 `deriveSubjectTreeArtifacts` function to
the API, existing request-file CLI and MCP `subject_tree`. It returns the native
result without writing or checking saved artifacts. It uses explicit structural
budgets and separate transport caps; incomplete native results are retained but
do not count as transport success. See the
[interface guide](../../payload/protocol/engine-interface.md) and
[P6 guide](ucs-1239-subject-views.md) for input and output details.

The first focused run passed **10/10** tree tests in **1774.253417 ms** on
Node **24.19.0**. `tests/engine-interface-tree.test.js` checks actual native/API/
request-CLI/official-SDK parity, specialized generated bytes versus stale-file
checks, complete/incomplete previews, empty versus absent/malformed authority,
Unicode proposals, exact tree/transport capacities, raw budget field refusal,
unchanged installation bytes and copied engine operation with only native YAML
dependencies. This is scoped evidence, not full combined acceptance or P4's
pending version-work acceptance.

Original red/green receipts remain in the P11 artifact directory as
`subject-tree-red.log`, `subject-tree-api-green.log`,
`subject-tree-transport-red.log` and `subject-tree-parity-first.log`.
The [existing interface decision](../../decisions/entries/ucs-1244-first-interface-contract-proposal.yaml)
records the agreement additively. Its proposal status remains unchanged.
No catalog, root README/AGENTS, package/lockfile, version or changelog edit is
part of this increment; main reconciles those with the actual integrated PR.
No new command shim, worker dispatch, schema, manifest path or protocol conduct
rule is introduced. The shipped interface/P6 guides are the affected usage
surfaces. Existing fixture/domain/experiment receipts remain untouched.

Prepared 2026-09-19; integrated 2026-09-20. The three proposed records and their catalog rows are now included in this repository. The preparation observations below retain their original revision and scope limits.

Update 2026-09-20: user authorization superseded the historical scope hold.
Integration now implements the [shared API/CLI/MCP operation set](../../payload/protocol/engine-interface.md).
The first-contract proposal records the subsequent choices, including retaining
native incomplete/refused reports. The preparation account below remains
historical evidence; it is not a current assertion that no interface exists.

Current implementation evidence: `tests/engine-interface.test.js` exercises
actual disk lookup/query, native intent execution, the official SDK stdio
client, copied engine operation without SDK dependencies, exact transport
capacities and all three reconsideration evidence families. The reviewed
combined run passed **122/122** in **6092.927167 ms** (Node 24.19.0), including
existing subject/intent CLI behavior, installation and module-load tests.
Local receipt: `local-history:unknown-knowledge-interface-reviewed-validation.log`.
This verifies these operations, not lifecycle publication or full acceptance.

Preserved development failures include the initial missing API (4 failing
tests), missing intent registrations, two installation checks requiring the
new bin inventory and conventional shim formatting, and the actual MCP
`__proto__` field-loss regression. The adapter now passes native documents
unchanged; a real stdio control followed by full API/MCP refusal equality
guards that correction. P2 reviewed the correction and found no further
interface issue. This does not replace source-specific lifecycle testing.

The next increment adds the existing P6 route/context owners and two fixed MCP
documentation resources. `tests/engine-interface-views.test.js` proves native
API/CLI/MCP parity, reversed-route IDs, zero counts, incomplete classification
coverage, retained material requirements and exact resource output capacity.
The combined interface/P6 run passed **52/52** in **3936.777459 ms** (Node
24.19.0); receipt: `local-history:unknown-knowledge-interface-views-validation.log`.
The missing-operation and missing-resource RED receipts are retained separately.
These additions leave tree publication and all remaining mutation bindings open.

The subsequent `record.preflight` binding calls the existing Ontology/Knowledge
owner with logging disabled. Native payloads, next actions, injected dates and
store degradation remain intact. Empty selections check store health only;
missing IDs preserve native refusal and existing unverified proposals preserve
unknown verdicts. The specialized CLI keeps its 0/1/2 contract; shared CLI/MCP
success requires native `data.ok: true`.

The isolated interface/preflight/module-load run passed **71/71** in
**6263.81275 ms**, including eleven new native/API/CLI/SDK tests and copied
engine operation without the MCP SDK. It used `293d043` plus eight named
interface/test/document overlays, excluding concurrent lifecycle edits.
The manifest and log are `local-history:unknown-knowledge-interface-preflight-freeze.json`
and `local-history:unknown-knowledge-interface-preflight-isolated.log`.
The initial ten missing-operation failures and a subsequent nine-pass/one-fail
test assumption about absent IDs remain separate receipts. The owner correctly
refused absent IDs; the corrected test distinguishes that from an existing
unverified proposal. Independent runtime review found no actionable issue.
This exposes an existing check; it adds no Decisions verdict, source-gather
operation, Subject budget claim or publication capability.

P11 has completed read-only source review, peer contract coordination, seven pinned consumer characterization checks, loader observations, an operation inventory and eight interface recommendations. P11 has implemented no public interface or binding and has opened no PR. Its implementation scope remains held pending the adjacent-scope answer. This document records P11's last delivered source observations; it makes no claim that those snapshots describe today's combined runtime.

## Proposed records and rationale

| Record after integration | Coverage and rationale |
|---|---|
| [Supported-interface scope](../../decisions/entries/ucs-1244-supported-interface-scope.yaml), `proposal:decision:d3ac3274-bb5e-4d45-824b-bc2a5ef63687` | Decision 1: separate adjacent interface ownership from P11 binding and keep P1–P10 independent. Team agreement on separation does not authorize additional scope. |
| [First interface contract](../../decisions/entries/ucs-1244-first-interface-contract-proposal.yaml), `proposal:decision:7253be98-5bf4-4cfb-a13a-cdb28663d2f0` | Decisions 2–6: nested ESM entry, explicit versioned request, actual static registrations, installed versus available authority, typed refusal/failure, and preservation of existing CLI output. Wire fields remain proposed. |
| [Captured evidence and acceptance](../../decisions/entries/ucs-1244-captured-evidence-and-acceptance.yaml), `proposal:decision:aa673542-1ffc-4fc7-b197-8831ce8f9b19` | Decisions 7–8: captured-model attribution, real owner evidence checks, observed behavioral acceptance and revision-specific test claims. |

The three entries use target schema version 2, exact proposal keys and `status: proposed`; no canonical slots, promotions or approval claims are introduced. The catalog handoff is additive. Main integrated entries, this document and additive catalog rows together without replacing other owners' changes. The old P11 checkout intentionally remains untouched because it still uses the pre-cutover format.

## Existing decision coverage

| Existing record | Applicability; no duplicate decision needed |
|---|---|
| [D-000007](../../decisions/entries/D-007-payload-allowlist.yaml) | Explicit payload allowlist; tests and evidence artifacts do not ship. A future public guide requires intentional manifest coverage. |
| [D-000010](../../decisions/entries/D-010-git-native-concurrency.yaml) | Git-native review and parallel ownership; proposed decisions are not canonical approval. |
| [D-000011](../../decisions/entries/D-011-verdicts-engine-conduct-protocol.yaml) | Actual engine verdicts and no cached trust; lookup/navigation never supplies evidence approval. |
| [D-000014](../../decisions/entries/D-014-engine-never-executes-client-code.yaml) | Deterministic read-only engine boundary, no client-code execution or network extension. |
| [D-000021](../../decisions/entries/D-021-version-policy.yaml) | Semver consequences and birth-stamp meaning, distinct from runtime identity. |
| [D-000022](../../decisions/entries/D-022-engine-is-javascript-jsdoc.yaml) | Actual JS ESM/JSDoc implementation, zero build; supersedes the earlier TypeScript wording in D-000002. |
| [PR completeness/versioning proposal](../../decisions/entries/pr-completeness-and-versioning.yaml), `proposal:decision:b97239f1-4802-4c2a-a8d5-21eeed73c018` | Direct user requirement: decisions, all relevant documentation and one version advance per actual PR. Its proposed publication status is separate from the user's instruction. Main owns shared version/root files. |

## Evidence index — retain original bytes

All original P11 artifacts are under `local-history:01a0b650-8b2e-7ed3-be7a-57a72894a158`. This local path is an audit location, not a seeded runtime dependency. No artifact was rewritten during this backfill.

| Original artifact | Evidence and limit |
|---|---|
| `UCS-1244-prep.md`, `UCS-1244-baseline-tests.log` | Initial scope/acceptance preparation and 97 passing baseline focused tests at the old checkout. No interface implementation proof. |
| `UCS-1234-peer-review-P11.md` | P1 consumer review: exact references, captured resolver data, ownership ambiguity, full-health separation. These are peer-owner decisions, not a P11 alternative identity implementation; P1 owns substantive identity records. |
| `UCS-1244-adjacent-minimum-prerequisite.md` | Narrow adjacent-slice proposal and P1/P2/P4 coordination. Exact interface fields unapproved. |
| `UCS-1244-lookup-binding-contract.md`, `lookup-consumer-contract.test.mjs`, `lookup-consumer-contract-f5660e9.log` | Seven actual P1/P2 consumer checks at `f5660e9`; characterization only, no feature red/green. |
| `lookup-loader-readiness.mjs`, `lookup-loader-readiness-f5660e9.json` | Actual old loader observation: identity/registry and CLI/API unavailable at that revision. Successful observation exit does not mean readiness and is not a permanent absence assertion. |
| `UCS-1244-readiness-9583f54.md` | Source readiness at `9583f54`, with explicitly separate pending-owner additions. |
| `UCS-1244-operation-inventory-reviewed-tips.md` | Exact CLI/domain inventory across named tips; explicitly not one tested combined snapshot. |
| `UCS-1244-interface-decision-proposals.md` | Eight detailed recommendations, with limited source comparison at `819ef85`. Combined tests were pending when written; no later result is inferred. |

The exact prior logs, test scripts, probes and document snapshots stay intact. Later implementation/status evidence must be added with its own revision and result. No trial, benchmark, experiment, runtime fixture or retained evidence was changed or rerun for this audit. P11 has no separately owned experiment to retune.

## Documentation and release coverage

| Surface | Current audit disposition |
|---|---|
| `decisions/entries/` and catalog | Three proposed entries and additive rows integrated here. Not present in the old P11 checkout. |
| `docs/agents/ucs-1244-interface-preparation.md` | This new audit supplies rationale, decision links, historical evidence and explicit limitations. |
| Root `AGENTS.md`, `CONTRIBUTING.md`, PR template, CI, package/lockfiles and changelog | Main-owned under the user instruction; no P11 shared-root edits. Main reported working rc.2 as Unreleased. This artifact handoff is not a separate PR or per-commit version bump. If integrated by PR, main applies the actual PR's version policy; no tag or publication follows implicitly. |
| Root README / CONTEXT and other README files | No P11 runtime feature, command or installation behavior changed, so no new usage claim is appropriate for this artifact-only backfill. Main can link this internal audit where needed; runtime-owner CLI documentation stays with those owners. |
| `payload/protocol/AGENTS.md`, other agent instructions and platform wrappers | No runtime loop, invocation or conduct change from P11. The proposed interface is not installed guidance. Future accepted binding must update actual affected protocol/wrapper references. |
| Existing P2 lookup, P4 query, P6 view and P5 intent guides | Previously source-reviewed and inventoried. P4's stale introductory CLI wording was reported and main assigned its correction to P4. P11 does not overwrite peer documents. |
| Future `payload/docs/engine-interface.md` and `cli/kit.manifest.yaml` | Still conditional on the adjacent implementation scope. No empty supported guide or manifest entry is added to imply delivery. |
| Schemas, templates and migration/authoring guides | No identity or data contract implementation change here; use existing schema-2 proposal format in the draft packet. No source schema or historical fixture modification. |
| Acceptance and benchmark documentation | Preserve pinned logs, settings and limitations. This audit records no new benchmark, host trial, public API acceptance or combined test pass. |

## Validation and remaining work

Validation completed against main's current target `validateStoreFile` implementation in `local-history:validate-record.js`: all three decision files and the catalog fragment returned `ok:true` with no errors. Exact catalog IDs/titles/files agreed, proposal IDs did not collide with the inspected main catalog, and every decision reference resolved against that catalog plus the additive packet. This is document-shape validation against the inspected working tree only, not runtime, governed publication or P11 acceptance. Main must still run its normal integrated store checks after additive insertion. No existing test or experiment was rerun or modified.

Remaining: scope authorization, assigned adjacent owner, agreement on the proposed contract, actual interface delivery, real P11 binding and CLI parity, copied-installation/read-only acceptance, and relevant docs/version updates with the actual PR. Broader subject interface acceptance must be assessed against UCS-1244, not assumed complete from a lookup-only slice.

## Subsequent read-only evidence inputs

The [reconsideration consumer contract](ucs-1237-reconsideration-consumers.md)
adds material capture input to the existing context/file-query libraries and
query/intent/view CLIs. Route/context views also receive original assessment
pairs. A future supported API/MCP binding must preserve equivalent evidence,
operation limits and unavailable outcomes; it must not silently omit these
inputs. This shipped CLI/library extension does not itself implement a supported
MCP server or close P11's interface acceptance. Earlier artifact receipts above
remain tied to their original snapshots.
