# UCS-1225 — cited review closes an organizational evidence gap

Specification: UCS-1225 and parent UCS-1217 in Linear. Review fixed point:
`200a01d29388604a4fe8263b633d0fdbb31e9d85`. Task branch:
`codex/ucs-1225-cited-human-review`.

## Scope

The existing engine already supplies the required capture helper, draft
preflight, catalog/typed references and derived checks. This change clarifies
capture and kb-build orchestration and supplies a reproducible A5 walkthrough.
It does not add an intake system, change ranking, expand the taxonomy, or claim
that structural validation certifies a human's review.

The new derivation example exposed a protocol-test side effect: the flag probe
ran `derive --write` in the kit checkout. It created `knowledge/derived`, which
made dogfood validation fail because there was no knowledge catalog. The probe
now runs each command in a disposable directory with cleanup. This was observed
before the fix, then the same focused protocol checks passed without leaving a
knowledge directory. Engine production code is unchanged.

## Actual fresh-agent observations

The controller used a disposable copy of the clean frontmatter-v2 fixture,
with a scoped component-telemetry interface and inventory that establish no
provider/safeguards. Sources and protocol were supplied; expected answers and
other trials' traces were not. Subagents were spawned with no inherited task
history. Evidence dates were injected as `2026-09-10`.

- Baseline: correctly reported provider and safeguards unestablished, searched
  only `src`/`docs`, and created a helper finding. It omitted residue/context:
  partial capture, not a complete ticket pass. It also tried the wrong scope
  path before recovering through survey-map; layout guidance belongs to the
  separate navigation ticket.
- Updated second and third trials: correctly retained unknowns, recorded
  permitted residue/context and consulted IDs/paths through the helper, and
  made no store mutations. The three findings represent independent resolution
  events, attested by the controller, not one session writing three files.
- Out-of-scope trial: employee dental coverage remained unestablished; no
  finding, excluded-path search, leaf, or taxonomy change was made.

Original trial root: `/tmp/ucs-1225-gap`. The first scratch setup briefly
tracked its dependency symlink and copied non-executable hooks. The hooks
were corrected before the first trial; the tracked symlink was removed after
the baseline exposed it as unsurveyed. The retained hook log proves actual
validation and reverse lookup on a Git commit. The committed setup helper
ignores that symlink and installs executable hooks before its first commit;
a separate reproduction run verified it.

## Verification discipline

This is protocol work measured at actual fresh-agent A5 seams. The original
CLI functionality was already green; no failing engine test or static prompt
assertion is represented as a model trial. The baseline capture omission and
unsafe CLI-probe write are the observed failures addressed here.

Default Git was 2.23.0 and rejected the existing test's `git init -b` flag.
The affected log-entry file passes using installed `/usr/bin/git` 2.50.1.
Final checks use `PATH=/usr/bin:$PATH` without changing the project toolchain.
No typecheck script is configured; no new typechecker was installed.

This small synthetic exercise cannot establish production knowledge,
large-corpus performance, or mechanical enforcement of citation approval.

The author trial found another protocol ambiguity: a valid draft leaf yields
`unknown`/exit 2, while AGENTS describes exit 2 broadly as STOP. The draft
passed structural and value checks and remained unpromoted. The controller
clarified that the already-authorized draft commit may proceed through hooks,
without relying on the leaf or promoting it. kb-build now makes this exact
boundary explicit; the assisted clarification is not represented as an
unassisted trial pass.

Checks after implementation: focused protocol files 26/26; focused engine
files 76/77 with the old-Git failure, then affected log-entry file 33/33 on
Git 2.50.1; full suite 1008/1008; lint 120 files/0 failures; automated acceptance
A1–A4/A6 pass; structural validator exit 0 (two existing loader warnings).
The full suite ran once. A5 results are recorded separately, not counted as CI.

## Human gate evidence

Fresh author created `L-000214` through kb-build, actually read
`docs/steward-interview.md`, and mapped each claim to its section. The leaf
uses existing governed facets and `interview` authority, scope/revision notes,
provenance, the catalog row, `concepts: [K-103]`, and the actual telemetry path.
No new vocabulary or unrelated leaf edge was added. Draft commit:
`4085fa0cdf9a1f9595eb4d04ff8336226936b241` in the disposable fixture.

The controller ran draft preflight, rejected the same three proposed findings
through the helper with a review-pending reason, and reran preflight. Both
verdicts were `unknown`/exit 2, with the leaf unchanged at draft. Rejection was
committed through the installed hooks as `10d287e`. This proves that the tested
rejection path did not promote the leaf; it does not claim the engine can
prevent an unauthorized author from editing a stage field.

The explicit controlled approval records the draft SHA-256
`a6a8d09cb78b9140ac952f0b4dd9a121cfe19c7f719b82bc30675d565b8c5a59` and source
SHA-256 `426064e3a83298a70577d9fb2324d1a0b2c91a5111dd52efe6f8faca74a20708`,
with a claim-to-source review and the narrow promotion authorization. The
controller reopened the original findings for the completed review; their
rejection history remains in commit `10d287e` and the raw rejection trace
(the helper removes `reason` from reopened YAML). Reopening is not independent
corroboration. No real organization, client, vendor or production policy was
approved.

Approved promotion was applied through kb-build in fixture commit
`fa021ce397d7327ae652cda999661992f4d38591`. The original body, scope and citations
were preserved; stage became verified with a dated revision, verification date
and static classification for this dated synthetic contract. This is not a
recommendation to classify real telemetry provider policy as static.
Structural/value validation, derive write/check with the same injected date,
and fresh leaf preflight all passed. Only then did helper transitions resolve
the original findings in `1dd959ae52dee41e6bf8a52bcc43efdbe992f10e`. Actual hooks
ran on both commits; `promotion.md` contains the observed outputs.

## Retrieval trial and repair

The first fresh post-promotion reader retrieved L-000214 and read the interview,
answered the fixture question correctly, and preserved production/enforcement
unknowns. It nevertheless preflighted K-102/K-103 only. This is an A5 failure
against the ticket's leaf-preflight criterion, not a complete retrieval pass.
`retrieval.md` retains that evidence. The capture section now explicitly tells
later gap-followup requests to preflight the consulted leaf itself and to
attribute both accession and source. General PREFLIGHT/conduct changes remain
with sibling tickets. A separate new fresh-agent repeat tests this repair.

The local kb-build clarification retains numeric exit-2 STOP for governed
retrieval and claim reliance. Only an explicit human authoring instruction
permits submitting an unpromoted, structurally valid draft for review; it does
not certify claims or permit promotion. It changes no general runtime conduct.

The second post-promotion fresh reader passed: a single actual preflight covered
K-102, K-103 and L-000214, yielding three trusted verdicts. It read the leaf and
its interview and attributed the answer to both. It explicitly retained the
production, retention, regional, certification/compliance and enforcement
limits. `retrieval-repeat.md` preserves the outputs and verbatim sources;
measured interval 64 seconds, excluding initial protocol read. The reminder was
then shortened to a canonical-step pointer for integration, so a final fresh
trial exercises that exact final wording rather than borrowing another ticket's
protocol. No combined-protocol success is attributed to this isolated change.

Reflect closeout committed its stamp at
`d27ab9020c8fdae51742b5533377c95eb414047f`: one item, one cycle, the earlier
rejection and later approval each counted once, zero archives and no trust
graduation. The trace explains that these are two decisions about R1, not
multiple items or votes. This exercise uses sequential local hooked commits;
no live PR, merge or rollout is claimed.

Final fresh trial at fixture HEAD
`5865481e1f4b69133b011778320a64dfd65d6b67` passed on the exact final isolated
protocol: concept and L-000214 preflight trusted, actual leaf/interview reads,
correct attributed answer and preserved unknowns. Measured interval: 76 seconds.
Final focused protocol rerun: 26/26; structural validation remains exit 0.

## Preserved evidence

The complete evidence index, clearly marked transcript excerpts, original failed
runs, review/source hashes, command outputs, checks and local fixture Git bundle
are preserved at:

`/Users/dimitriotero/.codex/visualizations/2026/09/10/01a088c8-315f-7141-ac9e-58d4287dbf2e/ucs-1225/README.md`

Reproduction starts from `acceptance/A5-knowledge-gap-walkthrough.md`. No model
identifier was pinned or exposed; fresh subagents inherited the session model
without task history. Success belongs to these controlled runs only. The
original September 9 evaluation files were not edited or preloaded into trials.
