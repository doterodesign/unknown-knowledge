# PR4: Reviewed migration and publication

Version `3.0.0-rc.5`, based on `6e4b409bfb5b23a61c6ad71d8441de8248065f53`. This is stage 4/7 of the dependent draft stack.

P7 and P1 validation transport: isolated migration and installation cutover, exact retained runtime evidence, fixed workers, fresh review and bounded candidate-reference publication.

Migration and publication libraries require their explicit review/runtime contracts. This PR does not run a customer migration, publish a package or approve a live candidate.

## Decisions and tradeoffs

- [retained runtime validation and publication](../../decisions/entries/retained-runtime-validation-and-publication.yaml)
- [ucs 1240 reviewed candidate publication](../../decisions/entries/ucs-1240-reviewed-candidate-publication.yaml)
- [ucs 1240 finite migration preservation](../../decisions/entries/ucs-1240-finite-migration-preservation.yaml)
- [ucs 1240 equivalent merge publication](../../decisions/entries/ucs-1240-equivalent-merge-publication.yaml)
- [ucs 1240 retirement publication](../../decisions/entries/ucs-1240-retirement-publication.yaml)
- [ucs 1240 split publication](../../decisions/entries/ucs-1240-split-publication.yaml)

These entries preserve their original proposal keys, catalog rows and historical reasoning. Runtime behavior is copied from the completed integration. The split follows dependency direction: each shared module appears before its consumers; fixed worker/retention families remain together because their dispatch validates the complete closed operation inventory. This avoids duplicating shared logic or changing product behavior to force ticket boundaries.

## Documentation and unchanged surfaces

Migration, preparation, retained/fresh review, installation cutover and publication guides describe the complete internal workflow. README/CONTEXT and acceptance add the exact fixed workers, historical runtime and offline inventory surface. Dependencies and agent wrapper commands remain unchanged. The execution guard restores its full literal inventories without broadening permitted execution.

Unavailable later operations are labeled in the [stage availability](README.md). Historical integration notes are retained as history, not presented as this head’s verification. [Original file hashes and modes](pr-04-ownership.json) identify the source slice; shared-root transitions deliberately reconcile current inventories and versions.

## Validation

[Portable validation receipt](pr-04-validation.json): 185/185 selected checks pass, zero failures/cancellations; lint checks 668 files with zero failures; automated A1–A4/A6 acceptance passes. A5 remains manual and unrun. Structural/value and immediate-base version checks pass. Final staged attribution and candidate gate are recorded with the exact head/base/tree in the PR description after staging. No hook bypass is used.

Earlier unchanged layers reuse their actual scope receipts; this is not a full-suite rerun or a new agent experiment. The original integration’s 3,878/3,906 run and 28 separately reconciled failures remain historical. Held-out completion regressed 15/18→10/18; no better-retrieval or overall nonregression claim is supported. The final approved packet and cross-PR coverage are consolidated in PR7. Original intermediate SHAs are local historical provenance, not assumed remotely available commits.

## Fixed installation asset dependency

The initial owning/shared run recorded901 tests:861 pass,40 fail. Thirty-eight direct failures arose from the absent fixed interface guide, one from its policy/manifest mismatch, and one was the failed parent of four affected cases. The unchanged fixed runtime inventory requires this guide, so its owning delivery moves from PR6 into PR4 with manifest inclusion and an explicit PR6 availability notice/heading. API, request CLI, MCP modules and dependencies still arrive in PR6; final guide wording is restored then. The receipt retains the initial exact source tree, log hash, all failure locations and original test list, alongside the corrected31-suite list and result. All fixed installation asset paths and local documentation links are present.

## Linux CI fixture corrections

The original public PR80 run completed with 3,819 tests: 3,811 passed and eight
failed because historical-runtime helpers assumed `/private/tmp` existed. The
helpers now use `tmpdir()` and retain their original prefixes and assertions.
PR81 and PR82 additionally exposed a direct loose-object read assumption in
the split-publication fault test. The shared disposable fixture disables
automatic Git collection and maintenance before committing; the test verifies
that the chosen source is unavailable to `cat-file` after deletion, then runs
the existing fresh-refusal and restoration checks. Explicit repacking reproduced
the storage assumption; the original CI maintenance mechanism was not observed.
The [correction receipt](pr-04-ci-correction.json) preserves all four original
run results and the distinction between observed failures and induced evidence.

These are fixture corrections within rc.5. Engine, CLI/API/MCP behavior, fixed
dispatch allowlists, schemas, manifests, migration instructions and agent wrappers
retain their reviewed contracts; their documentation claims do not change.
The existing proposed retained-runtime Decision records the rationale and limits.
