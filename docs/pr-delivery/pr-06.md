# PR6: Shared API, request CLI and local MCP

Version `3.0.0-rc.7`, based on `3bf63f16fadc2847fb67ccc37f6ee4b6b75e2159`. This is stage 6/7 of the dependent draft stack.

P11 plus the approved interface prerequisite: eleven shared read operations through an ESM API, request-file CLI and local stdio MCP; installed capability discovery and two fixed guide resources.

Migration and publication libraries require their explicit review/runtime contracts. This PR does not run a customer migration, publish a package or approve a live candidate.

## Decisions and tradeoffs

- [ucs 1244 supported interface scope](../../decisions/entries/ucs-1244-supported-interface-scope.yaml)
- [ucs 1244 first interface contract proposal](../../decisions/entries/ucs-1244-first-interface-contract-proposal.yaml)
- [ucs 1244 captured evidence and acceptance](../../decisions/entries/ucs-1244-captured-evidence-and-acceptance.yaml)

These entries preserve their original proposal keys, catalog rows and historical reasoning. Runtime behavior is copied from the completed integration. The split follows dependency direction: each shared module appears before its consumers; fixed worker/retention families remain together because their dispatch validates the complete closed operation inventory. This avoids duplicating shared logic or changing product behavior to force ticket boundaries.

## Documentation and unchanged surfaces

Package and both lock fields add only the approved MCP dependencies and two bins. Manifest, interface guide, README/CONTEXT, protocol and copied-installation assertions describe the actual API/CLI/MCP operations. Domain evaluators and stored schemas are reused unchanged; mutation/publication tools are not introduced. Existing platform wrappers point at the same canonical protocol.

Unavailable later operations are labeled in the [stage availability](README.md). Historical integration notes are retained as history, not presented as this head’s verification. [Original file hashes and modes](pr-06-ownership.json) identify the source slice; shared-root transitions deliberately reconcile current inventories and versions.

## Validation

[Portable validation receipt](pr-06-validation.json): 79/79 selected checks pass, zero failures/cancellations; lint checks 677 files with zero failures; automated A1–A4/A6 acceptance passes. A5 remains manual and unrun. Structural/value and immediate-base version checks pass. Final staged attribution and candidate gate are recorded with the exact head/base/tree in the PR description after staging. No hook bypass is used.

Earlier unchanged layers reuse their actual scope receipts; this is not a full-suite rerun or a new agent experiment. The original integration’s 3,878/3,906 run and 28 separately reconciled failures remain historical. Held-out completion regressed 15/18→10/18; no better-retrieval or overall nonregression claim is supported. The final approved packet and cross-PR coverage are consolidated in PR7. Original intermediate SHAs are local historical provenance, not assumed remotely available commits.
