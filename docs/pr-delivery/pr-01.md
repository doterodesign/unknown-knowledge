# PR1: canonical identity and store cutover

This intermediate `3.0.0-rc.2` tree supports new installations and isolated development. It does not provide the existing-store migration/cutover workflow, which arrives in PR4. No release or customer migration is authorized.

Permanent K/O/D identities, proposal keys and the installation ledger replace positional record IDs. The actual loader carries optional Subject governance, whole-record assignment state and retained assignment history because the structural validator depends on those modules. Canonical schemas, initializers, fixtures, logging and authoring guidance change together.

The final loader has a real import cycle across Subject authority, governance, refusal assessments and reconsideration evidence. Keeping that closure together avoids an intermediate reader that cannot load its own stores. Query execution, lifecycle domain gates, migration/publication workers and API/MCP transports remain later layers. Reopened log outcomes stay with canonical logging; the larger reflection procedure arrives in PR5.

## Decisions and documentation

- [Identity and cutover rationale](../../decisions/entries/ucs-1234-identities-runtime-promotion.yaml), including the one-time matching-reader verification method.
- [Subject governance](../../decisions/entries/subject-governance-contracts.yaml), [assignment state](../../decisions/entries/whole-record-subject-assignments.yaml) and [history](../../decisions/entries/ucs-1241-assignment-history-and-gates.yaml).
- [Navigation](../../decisions/entries/retrieval-envelope-navigation.yaml), [reopened outcome rationale](../../decisions/entries/retrieval-review-state.yaml) and [PR completeness](../../decisions/entries/pr-completeness-and-versioning.yaml).

All retain their original proposal identities and catalog rows. No canonical approval is invented. The reasons and alternatives remain in the original entries; packaging adds the explicit format-transition disposition to the identity entry.

README and CONTEXT list the commands actually present. Root and protocol AGENTS, wrappers, store schemas, templates, canonical acceptance fixtures, contributor policy and the version CI guard accompany the cutover. Migration documentation limits this stage to new installations. Final-design and historical documentation is explicitly labeled; unavailable later links do not masquerade as current usage. There are no new API/MCP bins or dependencies at this stage. The manifest omits later guides; the execution guard admits no prepared worker yet. Publishing policy remains gated and no release metadata is created.

## Validation

[Portable result and original-log hashes](pr-01-validation.json) preserve both failed assembly attempts. The first run was 1,756/1,792. The corrected frozen run was 1,785/1,787, with two remaining obsolete Swift fixture expectations; all six Swift checks pass after restoring the reference's canonical test. No runtime change was needed. The 87 focused documentation/contract checks also pass. This is combined evidence, not a claimed all-green full rerun.

Lint checked 240 files with zero failures. Automated acceptance A1–A4/A6 passes; A5 is manual and unrun. The initial acceptance failure required narrowing the exact inventory to this stage's absent migration workers. Structural and value checks and the rc.1-to-rc.2 guard pass. Final matching-reader attribution, candidate commit gate and exact tree associations are recorded in the PR description after staging.

The literal current-reader attribution initially exited zero with a healthy candidate and an unhealthy old-format before tree (159 errors). That before result is unavailable attribution under this reader, never a healthy pass. The approved one-time check runs the identical complete changed-path set against each immutable tree using its matching runtime and dependencies, plus the normal current candidate/index commit gate. Later canonical PR bases use the normal one-runtime check. Hooks and runtime exit codes are unchanged.

Four trailing-blank-line warnings in imported source/helpers are preserved from the reference instead of making unrelated source edits. Original source hashes and file modes are in the [ownership ledger](pr-01-ownership.json). Shared files have deliberate intermediate inventories and documentation transitions; their final restoration is checked across the stack.

## Evidence limits

The original integration's deterministic run remains 3,878/3,906 with 28 separately reconciled failures, not a single green run. Its full final evaluation belongs in PR7. Observed held-out completion regressed from 15/18 to 10/18; no better-retrieval or overall nonregression claim is supported. Old intermediate SHAs are local historical provenance, not remotely available source links.

## CI harness reconciliation

Original failed run: https://github.com/doterodesign/unknown-knowledge/actions/runs/35735220372. The snapshot-failure hook assertions completed, but temporary Git repository teardown raised ENOTEMPTY on Linux. Recursive removal now uses three bounded retries at 100 ms backoff; final errors remain visible. This follows the existing commit-snapshot cleanup policy and changes no runtime or snapshot-cleanliness assertion. This internal correction keeps the same PR version. See [CI correction receipt](pr-01-ci-correction.json).

A subsequent Linux run still failed during teardown inside `.git/objects` ([run35737013958](https://github.com/doterodesign/unknown-knowledge/actions/runs/35737013958)); PR2 observed the same class ([run35737252373](https://github.com/doterodesign/unknown-knowledge/actions/runs/35737252373)). Local Git Trace2 confirms setup commits launch automatic maintenance with `--detach`. Fixture-local `gc.autoDetach=false` and `maintenance.autoDetach=false` change that invocation to `--no-detach`. Automatic maintenance stays enabled; all hook and snapshot assertions and final teardown errors remain. The local trace supports the lifecycle correction but does not reproduce or prove the Linux failure cause. Original failed runs remain evidence pending fresh Linux CI.
