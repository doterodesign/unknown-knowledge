# PR5: Reflection and evidence conduct

Version `3.0.0-rc.6`, based on `febfa8d594dcc77b904e8a90f658f4d7453d2cc5`. This is stage 5/7 of the dependent draft stack.

P9: durable reflection review, evidence accounting, resume/heartbeat and source-review conduct. Canonical logging and prior outcomes already arrived with PR1.

Migration and publication libraries require their explicit review/runtime contracts. This PR does not run a customer migration, publish a package or approve a live candidate.

## Decisions and tradeoffs

- [retrieval review state](../../decisions/entries/retrieval-review-state.yaml)
- [retrieval envelope navigation](../../decisions/entries/retrieval-envelope-navigation.yaml)

These entries preserve their original proposal keys, catalog rows and historical reasoning. Runtime behavior is copied from the completed integration. The split follows dependency direction: each shared module appears before its consumers; fixed worker/retention families remain together because their dispatch validates the complete closed operation inventory. This avoids duplicating shared logic or changing product behavior to force ticket boundaries.

## Documentation and unchanged surfaces

The reflect skill and its corresponding procedure tests carry durable per-item review and bounded archival conduct. Root navigation and existing proposed Decisions already establish the applicable gates. No engine API, schema, manifest, dependency or CLI bin changes; existing seeded skill inclusion ships the updated procedure.

Unavailable later operations are labeled in the [stage availability](README.md). Historical integration notes are retained as history, not presented as this head’s verification. [Original file hashes and modes](pr-05-ownership.json) identify the source slice; shared-root transitions deliberately reconcile current inventories and versions.

## Validation

[Portable validation receipt](pr-05-validation.json): 64/64 selected checks pass, zero failures/cancellations; lint checks 668 files with zero failures; automated A1–A4/A6 acceptance passes. A5 remains manual and unrun. Structural/value and immediate-base version checks pass. Final staged attribution and candidate gate are recorded with the exact head/base/tree in the PR description after staging. No hook bypass is used.

Earlier unchanged layers reuse their actual scope receipts; this is not a full-suite rerun or a new agent experiment. The original integration’s 3,878/3,906 run and 28 separately reconciled failures remain historical. Held-out completion regressed 15/18→10/18; no better-retrieval or overall nonregression claim is supported. The final approved packet and cross-PR coverage are consolidated in PR7. Original intermediate SHAs are local historical provenance, not assumed remotely available commits.
