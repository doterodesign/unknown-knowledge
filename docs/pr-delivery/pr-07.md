# PR7: Evaluation and portable final review

Version `3.0.0-rc.8`, based on `5ac00e46c177f89f08c91da38140b7185f1f78d6`. This is stage 7/7 of the dependent draft stack.

P10 and cross-stack evidence: independent deterministic evaluation tools, public synthetic corpus, preserved public/held-out results and the approved anonymous portable final packet.

Migration and publication libraries require their explicit review/runtime contracts. This PR does not run a customer migration, publish a package or approve a live candidate.

## Decisions and tradeoffs

- [retrieval evaluation custody](../../decisions/entries/retrieval-evaluation-custody.yaml)
- [retrieval independent scoring](../../decisions/entries/retrieval-independent-scoring.yaml)
- [retrieval fixture custody](../../decisions/entries/retrieval-fixture-custody.yaml)
- [retrieval operational qualification](../../decisions/entries/retrieval-operational-qualification.yaml)
- [retrieval repair evidence](../../decisions/entries/retrieval-repair-evidence.yaml)

These entries preserve their original proposal keys, catalog rows and historical reasoning. Runtime behavior is copied from the completed integration. The split follows dependency direction: each shared module appears before its consumers; fixed worker/retention families remain together because their dispatch validates the complete closed operation inventory. This avoids duplicating shared logic or changing product behavior to force ticket boundaries.

## Documentation and unchanged surfaces

Retrieval acceptance reports, evidence navigation, proposed evaluation Decisions and the final coverage ledger consolidate implemented capabilities and observed failures. Engine, schemas, public API/CLI/MCP and wrapper behavior are unchanged. The approved report copies replace local locators and carry independent sanitized hashes; original artifacts/pins/outcomes remain preserved.

Unavailable later operations are labeled in the [stage availability](README.md). Historical integration notes are retained as history, not presented as this head’s verification. [Original file hashes and modes](pr-07-ownership.json) identify the source slice; shared-root transitions deliberately reconcile current inventories and versions.

## Validation

[Portable validation receipt](pr-07-validation.json): 99/99 selected checks pass, zero failures/cancellations; lint checks 689 files with zero failures; automated A1–A4/A6 acceptance passes. A5 remains manual and unrun. Structural/value and immediate-base version checks pass. Final staged attribution and candidate gate are recorded with the exact head/base/tree in the PR description after staging. No hook bypass is used.

Earlier unchanged layers reuse their actual scope receipts; this is not a full-suite rerun or a new agent experiment. The original integration’s 3,878/3,906 run and 28 separately reconciled failures remain historical. Held-out completion regressed 15/18→10/18; no better-retrieval or overall nonregression claim is supported. The final approved packet and cross-PR coverage are consolidated in PR7. Original intermediate SHAs are local historical provenance, not assumed remotely available commits.

## Exact coverage and preserved provenance

The [final coverage ledger](final-coverage.json) covers all1,580 intended paths:1,529 match the reference exactly;51 specific packaging exceptions are checked, with zero unexpected implementation differences and zero unrelated final baseline changes. It records intermediate transitions for37 historical documents and proves their final bytes are restored. The three test harness corrections are exact reviewed transformations. Every sanitized documentation transformation is reconstructed byte-for-byte from original source; package versions, changelog policy/history, README delivery note and the existing proposed Decision disposition are separately checked. Final product protocol and engine bytes match the reference.

The [approved anonymous packet](../../acceptance/retrieval/review-packet/README.md) contains14 approved report copies plus copy provenance/navigation. Every original and portable hash is verified. Only local evidence locators and declared copy metadata change; outcome numbers, pins and adverse findings remain. No private source bindings were opened. The42 additional review artifacts are confined to this delivery directory and the approved packet; final exact-head inventory is supplied separately to avoid a self-referential hash.

The initial precommit run was98/99: the real Git replacement test cloned the current committed PR6 head before the new harness directory existed. The local PR7 candidate was then committed without changing the test. All99 checks pass at `5d9f2d127c83c376d8a4e96f8b7c21b951600bee` / tree `6435fe76252459d2f107a9eb54965ef16f0d7d54`. The receipt preserves the initial failure and exact tested candidate; the final follow-up only records evidence. This is deterministic fixture testing, not a rerun of the reader evaluation.
