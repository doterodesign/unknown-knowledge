# PR2: Governed retrieval, intent and derived views

Version `3.0.0-rc.3`, based on `62622b3fb8725d20c919b7c7589449483889653e`. This is stage 2/7 of the dependent draft stack.

P4/P5/P6: captured Subject queries, strong Kleene unknowns, explicit operation limits, intent validation/execution, metadata lookup, structural trees and route/context views.

Existing-store migration is unavailable until PR4; intermediate versions are for new installations and isolated development.

## Decisions and tradeoffs

- [captured subject query semantics](../../decisions/entries/captured-subject-query-semantics.yaml)
- [subject operation admission](../../decisions/entries/subject-operation-admission.yaml)
- [intent retrieval contracts](../../decisions/entries/intent-retrieval-contracts.yaml)
- [intent query execution contract](../../decisions/entries/intent-query-execution-contract.yaml)
- [optional subject retrieval routing](../../decisions/entries/optional-subject-retrieval-routing.yaml)
- [subject views and contexts](../../decisions/entries/subject-views-and-contexts.yaml)
- [lossless subject query output](../../decisions/entries/lossless-subject-query-output.yaml)

These entries preserve their original proposal keys, catalog rows and historical reasoning. Runtime behavior is copied from the completed integration. The split follows dependency direction: each shared module appears before its consumers; fixed worker/retention families remain together because their dispatch validates the complete closed operation inventory. This avoids duplicating shared logic or changing product behavior to force ticket boundaries.

## Documentation and unchanged surfaces

README, CONTEXT, canonical AGENTS, intent guide, manifest and owner query/view/intent contracts now describe the present CLI layer. Identity/store schemas, wrapper pointers, package dependencies and publishing workflow keep the previous stage’s contracts; no API/MCP bin is declared.

Unavailable later operations are labeled in the [stage availability](README.md). Historical integration notes are retained as history, not presented as this head’s verification. [Original file hashes and modes](pr-02-ownership.json) identify the source slice; shared-root transitions deliberately reconcile current inventories and versions.

## Validation

[Portable validation receipt](pr-02-validation.json): 544/544 selected checks pass, zero failures/cancellations; lint checks 323 files with zero failures; automated A1–A4/A6 acceptance passes. A5 remains manual and unrun. Structural/value and immediate-base version checks pass. Final staged attribution and candidate gate are recorded with the exact head/base/tree in the PR description after staging. No hook bypass is used.

Earlier unchanged layers reuse their actual scope receipts; this is not a full-suite rerun or a new agent experiment. The original integration’s 3,878/3,906 run and 28 separately reconciled failures remain historical. Held-out completion regressed 15/18→10/18; no better-retrieval or overall nonregression claim is supported. The final approved packet and cross-PR coverage are consolidated in PR7. Original intermediate SHAs are local historical provenance, not assumed remotely available commits.
