# KK-26 drift fixture: K-100's enumerates claim drifted from its source (the
# acceptance case — quarantined), K-110 is untouched (must stay trusted), and
# K-130 is draft (unknown, §3.5).
schema-version: 1
entries:
  - id: K-100
    term: Icon
    class: 100-registries
    summary: An icon the product's icon set ships.
    source-of-truth: [src/icons.txt]
    status: active
    enumerates:
      - kind: test-lines
        source: src/icons.txt
        values: [grid, list, star]
  - id: K-110
    term: Color token
    class: 100-registries
    summary: A base color token.
    source-of-truth: [src/color-tokens.txt]
    status: active
    enumerates:
      - kind: test-lines
        source: src/color-tokens.txt
        values: [srgb, p3]
  - id: K-130
    term: Draft panel
    class: 100-registries
    summary: A concept still being drafted.
    source-of-truth: [src/icons.txt]
    status: draft
    enumerates:
      - kind: test-lines
        source: src/icons.txt
        values: [not-in-the-source]
