# Derived-layer fixture (UCS-1158) — the DETERMINISM TWIN of ../store.
#
# Same six leaves, same facets, same everything a tree projects — and a
# different order everywhere the order is not the content: the catalog rows run
# backwards, and the leaf FILENAMES are renamed so the loader's directory walk
# hands them over in a different sequence than in the twin.
#
# The trees generated from this store must be BYTE-IDENTICAL to the trees
# generated from ../store, except for the `file` values in index.json, which
# name real paths and are content rather than order. That is the stable-sort
# claim stated as a fixture: a browse tree's shape must come from the store's
# facts, never from the order the filesystem happened to enumerate them in.
schema-version: 2
store: knowledge
entries:
  - id: K-000004
    title: Brand color token table
    file: brand/1-brand-color-token-table.md
  - id: proposal:knowledge:21321321-3213-4213-8213-213213213213
    title: Component render budget
    file: design-system/1-component-render-budget.md
  - id: proposal:knowledge:17117117-1171-4171-8171-171171171171
    title: Visual regression triage playbook
    file: engineering/2-visual-regression-triage-playbook.md
  - id: K-000003
    title: Bundle-size budget thresholds
    file: engineering/3-bundle-size-budget-thresholds.md
  - id: K-000002
    title: Contrast ratios at token export
    file: design-system/2-contrast-ratios-at-token-export.md
  - id: K-000001
    title: Icon component sizing quirks
    file: design-system/3-icon-component-sizing-quirks.md
