# Time facet fixture (UCS-1150) — the concept all seven leaves are entry
# points for, so one resolver query ranks the whole boundary set at once.
schema-version: 2
store: ontology
entries:
  - id: O-000001
    title: Freshness boundary
    file: classes/100-freshness.yaml
