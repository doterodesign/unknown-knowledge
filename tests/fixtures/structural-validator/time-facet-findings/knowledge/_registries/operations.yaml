# Frontmatter v2 fixture (UCS-1149) — the operations registry (verbs).
schema-version: 1
store: knowledge
registry: operations
values:
  - value: onboard-provider
    gloss: Bring a new odds-feed provider into the ingest pipeline.
    warrant: L-000117 documents what onboarding a provider must account for.
    decision: D-401
    minted: "2026-08-16"
