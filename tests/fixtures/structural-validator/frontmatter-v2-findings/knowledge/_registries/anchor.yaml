# Frontmatter v2 fixture (UCS-1149) — the anchor registry.
#
# The three truth anchors are D-003's, adopted here by D-401 rather than
# invented: a project mints these, it does not choose them.
schema-version: 1
store: knowledge
registry: anchor
values:
  - value: artifact
    gloss: The repo decides. The claim is checkable against code, config, or schema.
    warrant: L-000213's budget is enforced in src/feed/latency.ts.
    decision: D-401
    minted: "2026-08-16"
  - value: world
    gloss: An outside authority decides — a standards body, a vendor, a spec.
    warrant: L-000117's claim is settled by the component kit's own API documentation.
    decision: D-401
    minted: "2026-08-16"
  - value: team
    gloss: The team decided. The claim is true because it was chosen, not discovered.
    warrant: D-401 itself is a team-anchored fact this store must be able to classify.
    decision: D-401
    minted: "2026-08-16"
