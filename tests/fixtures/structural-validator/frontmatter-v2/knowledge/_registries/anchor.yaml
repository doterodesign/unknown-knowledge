# Frontmatter v2 fixture (UCS-1149) — the anchor registry.
#
# The three truth anchors come from kit decision D-000003; this fixture's
# D-000001 adopts them rather than inventing them.
schema-version: 2
store: knowledge
registry: anchor
values:
  - value: artifact
    gloss: The repo decides. The claim is checkable against code, config, or schema.
    warrant: proposal:knowledge:21321321-3213-4213-8213-213213213213's budget is enforced in src/feed/latency.ts.
    decision: D-000001
    minted: "2026-08-16"
  - value: world
    gloss: An outside authority decides — a regulator, a vendor, a standard.
    warrant: K-000001's claim is settled by the vendor's own API documentation.
    decision: D-000001
    minted: "2026-08-16"
  - value: team
    gloss: The team decided. The claim is true because it was chosen, not discovered.
    warrant: D-000001 itself is a team-anchored fact this store must be able to classify.
    decision: D-000001
    minted: "2026-08-16"
