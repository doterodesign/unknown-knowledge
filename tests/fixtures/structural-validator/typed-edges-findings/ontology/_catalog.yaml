# Typed edges fixture (UCS-1151) — the two ID-SPACE families, each dangling.
#
# The ontology is inherited from the clean fixture and is incidental here: this
# store carries ONE leaf, whose `concepts` and `relates.depends-on` both name
# ids nothing mints. Neither O-000001 nor O-000002 is referenced by it — the point of
# the store is the two unresolved-ref findings, not the concepts themselves.
#
# The `via` field and the structural-vs-textual join are exercised in the clean
# fixture, which has the leaves to do it with — not here.
schema-version: 2
store: ontology
entries:
  - id: O-000001
    title: Retrieval ranking
    file: classes/200-retrieval.yaml
  - id: O-000002
    title: Index build
    file: classes/200-retrieval.yaml
