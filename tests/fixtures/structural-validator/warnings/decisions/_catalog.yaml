# D-000001 is mid-import: the documented pending marker (catalog schema) —
# a warning, never a blocking index-drift.
schema-version: 2
store: decisions
entries:
  - id: D-000001
    title: Declared but pending import
    file: pending-import
