# Deprecated concept with a deleted SSOT path — §3.5 demotes the blocking
# path-existence check to a warning (the source-deletion escape hatch).
schema-version: 2
entries:
  - id: O-000001
    term: Legacy audit log
    class: 800-legacy
    summary: Deprecated concept whose source file was legitimately deleted.
    source-of-truth: [src/legacy/audit-log.ts]
    status: deprecated
