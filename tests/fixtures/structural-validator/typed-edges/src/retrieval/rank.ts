// Typed edges fixture (UCS-1151): the artifact under the `src/retrieval` folder
// pointer. Both K-201's source-of-truth and L-000501's `paths` name that
// directory, which is what lets one path exercise both joins at once.
export const rank = () => 0;
