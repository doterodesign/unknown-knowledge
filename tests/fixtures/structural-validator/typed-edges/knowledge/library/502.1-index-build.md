---
schema-version: 3
id: K-000006
notation: "502.1"
domain: library
heading: Index build batch
concepts: [O-000002]
paths:
  - src/index/build.ts
citations:
  - source: Index pipeline runbook 2026-08-01
    accessed: "2026-08-01"
terms: [Index build]
---

The nightly batch that materializes the searchable index. This leaf reaches
O-000002 BOTH ways — its `terms` name the concept's term and it declares the
concept id — which is what pins the tie-break: the structural claim wins, so
`via` reads `declared`.
