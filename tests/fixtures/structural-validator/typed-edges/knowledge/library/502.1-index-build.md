---
schema-version: 2
id: L-000510
notation: "502.1"
domain: library
heading: Index build batch
concepts: [K-202]
paths:
  - src/index/build.ts
citations:
  - source: Index pipeline runbook 2026-08-01
    accessed: "2026-08-01"
terms: [Index build]
---

The nightly batch that materializes the searchable index. This leaf reaches
K-202 BOTH ways — its `terms` name the concept's term and it declares the
concept id — which is what pins the tie-break: the structural claim wins, so
`via` reads `declared`.
