# Typed edges fixture (UCS-1151). Two concepts, and the split between them is
# the point: O-000001 is named by no leaf's `terms` at all, so the ONLY way a leaf
# reaches it is the structural `concepts` edge. O-000002 is reachable both ways,
# which is what lets the `via` field be tested for both values in one store.
schema-version: 2
store: ontology
entries:
  - id: O-000001
    title: Retrieval ranking
    file: classes/200-retrieval.yaml
  - id: O-000002
    title: Index build
    file: classes/200-retrieval.yaml
