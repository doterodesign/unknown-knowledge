---
schema-version: 1
id: L-000602
notation: "600.2"
domain: widgets
heading: Unminted domain parent
facets:
  domain: sprockets/care
citations:
  - source: Gadget registry operating manual 2026
    accessed: "2026-07-07"
    authority: primary
---

PLANTED: hierarchical membership. `sprockets/care` names a two-segment path
whose TOP segment `sprockets` was never minted, so the child cannot hang off
it. The finding must name the missing SEGMENT, not the whole path — the
segment is the registry edit the author can actually make.
