---
schema-version: 1
notation: "600.3"
domain: widgets
heading: Unminted domain leaf segment
facets:
  domain: widgets/packaging
citations:
  - source: Gadget registry operating manual 2026
    accessed: "2026-07-07"
    authority: primary
---

PLANTED: the PARENT `widgets` is minted, the child `packaging` is not. A
hierarchy that accepted this would be a set of strings containing slashes, so
the finding names `widgets/packaging` as the unminted segment.
