# Resolver fixture whose source-of-truth pointers EXIST ON DISK (UCS-933), so
# the folder-pointer test can consult the filesystem rather than the name.
schema-version: 2
store: ontology
entries:
  - id: O-000001
    title: Api surface
    file: classes/100-api.yaml
  - id: O-000002
    title: Model
    file: classes/100-api.yaml
