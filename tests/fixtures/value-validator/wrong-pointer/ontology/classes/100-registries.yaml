# KK-07 wrong-pointer fixture: the file EXISTS and parses, but carries none of
# the claimed values — the wrong-pointer signature (PRD §4), one distinct
# finding instead of a per-value cascade.
schema-version: 1
entries:
  - id: K-100
    term: Icon
    class: 100-registries
    summary: An icon the product's icon set ships.
    source-of-truth: [src/registry.txt]
    status: active
    enumerates:
      - kind: test-lines
        source: src/registry.txt
        values: [grid, list]
