# KK-07 clean fixture: K-100 agrees with its source (values are compared as
# SETS — the source lists them in a different order); K-130 is draft, so its
# enumerates are skipped (§3.5 structural checks only) even though its source
# file does not exist.
schema-version: 1
entries:
  - id: K-100
    term: Sport
    class: 100-registries
    summary: A sport vertical the product serves.
    source-of-truth: [src/sports.txt]
    status: active
    enumerates:
      - kind: test-lines
        source: src/sports.txt
        values: [nfl, nba, mlb]
  - id: K-130
    term: Draft vertical
    class: 100-registries
    summary: A concept still being drafted.
    source-of-truth: [src/missing.txt]
    status: draft
    enumerates:
      - kind: test-lines
        source: src/missing.txt
        values: [x]
